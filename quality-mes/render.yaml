services:
  - type: web
    name: quality-mes
    env: python
    plan: free
    buildCommand: pip install -r quality-mes/backend/requirements.txt && cd quality-mes/frontend && npm install && npm run build
    startCommand: cd quality-mes/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
